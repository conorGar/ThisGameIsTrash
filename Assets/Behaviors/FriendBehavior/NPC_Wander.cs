using UnityEngine;
using System.Collections;

[RequireComponent(typeof(IsometricSorting))]
[RequireComponent(typeof(EnemyPath))]
public class NPC_Wander : MonoBehaviour
{

	// Use this for initialization
	void Start ()
	{
	
	}
	
	// Update is called once per frame
	void Update ()
	{
	
	}
}

